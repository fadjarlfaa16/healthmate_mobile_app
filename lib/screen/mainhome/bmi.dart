import 'package:flutter/material.dart';

class BMIPredictionPage extends StatelessWidget {
  const BMIPredictionPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      // appBar: AppBar(title: Text("BMI Prediction")),
      body: Center(child: Text("This is the BMI Prediction Page")),
    );
  }
}
